const User=require("../models/user"),Visit=require("../models/visit"),VisitRevision=require("../models/visitRevision.model"),Item=require("../models/item.model"),ItemRevision=require("../models/itemRevision.model"),UserVisitPreference=require("../models/userVisitPreference.model"),AppError=require("../utils/AppError");
const {getActiveUserOrFail}=require("./museumAuthorization.service"),{getMuseumVocabulary}=require("./museumVocabulary.service"),{assertPreferenceValue,findRepresentationByPolicy,findDefaultRepresentation,resolveCommunityRepresentation}=require("./presentationPolicy.service"),{listRepresentationCandidates}=require("./presentationModel.service");
function normalizeAbstractPreference(payload={}){const preference={depthPreference:Number(payload.depthPreference),languageComplexityPreference:Number(payload.languageComplexityPreference)};assertPreferenceValue(preference.depthPreference,"depthPreference");assertPreferenceValue(preference.languageComplexityPreference,"languageComplexityPreference");return preference}
async function setUserDefaultPreference({userId,payload}){const user=await getActiveUserOrFail(userId);user.defaultPresentationPreference=payload===null||payload?.clear===true?null:normalizeAbstractPreference(payload);await user.save();return user.defaultPresentationPreference}
async function getPublishedVisitOrFail(visitId){const visit=await Visit.findOne({_id:visitId,lifecycleStatus:"active",publishedRevisionId:{$ne:null}});if(!visit)throw new AppError("Visita pubblicata non trovata",404);const revision=await VisitRevision.findById(visit.publishedRevisionId);if(!revision)throw new AppError("Revisione pubblicata della visita non trovata",404);return{visit,revision}}
async function assertOfficialPolicyAvailable({revision,policy}){for(let index=0;index<revision.stops.length;index+=1){const item=await Item.findOne({_id:revision.stops[index].itemId,lifecycleStatus:"active"}).lean();if(!item?.publishedRevisionId)throw new AppError("La visita contiene un item non disponibile",409);const itemRevision=await ItemRevision.findById(item.publishedRevisionId).lean();if(!findRepresentationByPolicy(itemRevision,policy))throw new AppError("La preferenza non e disponibile in tutte le tappe",400,[{field:`stops[${index}]`,code:"PREFERENCE_NOT_AVAILABLE",message:"La combinazione scelta non e disponibile per questa tappa",context:{itemId:item._id,...policy}}])}}
async function setVisitPreference({userId,visitId,payload}){await getActiveUserOrFail(userId);const{visit,revision}=await getPublishedVisitOrFail(visitId),mode=payload?.mode==="custom"?"custom":"default",data={userId,visitId,mode,durationKey:null,languageLevelKey:null,depthPreference:null,languageComplexityPreference:null};if(mode==="custom"&&visit.kind==="official"){const vocabulary=await getMuseumVocabulary(visit.ownerMuseumId),policy={durationKey:String(payload.durationKey||"").trim().toLowerCase(),languageLevelKey:String(payload.languageLevelKey||"").trim().toLowerCase()};if(!vocabulary.durationTypes.some(e=>e.key===policy.durationKey))throw new AppError("durationKey non valida",400);if(!vocabulary.languageLevels.some(e=>e.key===policy.languageLevelKey))throw new AppError("languageLevelKey non valida",400);await assertOfficialPolicyAvailable({revision,policy});Object.assign(data,policy)}if(mode==="custom"&&visit.kind==="community")Object.assign(data,normalizeAbstractPreference(payload));return UserVisitPreference.findOneAndUpdate({userId,visitId},{$set:data},{upsert:true,new:true,runValidators:true})}
async function getEffectivePreference({userId,visit}){const[user,visitPreference]=await Promise.all([User.findById(userId).lean(),UserVisitPreference.findOne({userId,visitId:visit._id}).lean()]);if(visit.kind==="official")return visitPreference?.mode==="custom"?{mode:"custom",durationKey:visitPreference.durationKey,languageLevelKey:visitPreference.languageLevelKey}:{mode:"default"};if(visitPreference?.mode==="custom")return{mode:"custom",depthPreference:visitPreference.depthPreference,languageComplexityPreference:visitPreference.languageComplexityPreference};if(user?.defaultPresentationPreference)return{mode:"user_default",...user.defaultPresentationPreference};return{mode:"item_default"}}
async function getStoredVisitPreference({userId,visitId}){await getActiveUserOrFail(userId);await getPublishedVisitOrFail(visitId);return UserVisitPreference.findOne({userId,visitId}).lean()}
async function getVisitPreferenceOptions({userId,visitId}){await getActiveUserOrFail(userId);const{visit,revision}=await getPublishedVisitOrFail(visitId);if(visit.kind==="community")return{kind:"community",dimensions:{depthPreference:{min:0,max:1},languageComplexityPreference:{min:0,max:1}},fallback:"item_default"};const vocabulary=await getMuseumVocabulary(visit.ownerMuseumId);let commonPairs=null;for(const stop of revision.stops){const item=await Item.findOne({_id:stop.itemId,lifecycleStatus:"active"}).lean();if(!item?.publishedRevisionId)throw new AppError("La visita contiene un item non disponibile",409);const itemRevision=await ItemRevision.findById(item.publishedRevisionId).lean(),pairs=new Set(listRepresentationCandidates(itemRevision).map(e=>`${e.representation.durationKey}::${e.representation.languageLevelKey}`));commonPairs=commonPairs===null?pairs:new Set([...commonPairs].filter(pair=>pairs.has(pair)))}return{kind:"official",defaultPresentationPolicy:revision.defaultPresentationPolicy,durationTypes:vocabulary.durationTypes,languageLevels:vocabulary.languageLevels,combinations:[...(commonPairs||new Set())].map(pair=>{const[durationKey,languageLevelKey]=pair.split("::");return{durationKey,languageLevelKey}})}}
async function buildPresentationPlan({userId,visitId}){await getActiveUserOrFail(userId);const{visit,revision}=await getPublishedVisitOrFail(visitId),effectivePreference=await getEffectivePreference({userId,visit}),stops=[];let estimatedContentSeconds=0;for(let index=0;index<revision.stops.length;index+=1){const stop=revision.stops[index],item=await Item.findOne({_id:stop.itemId,lifecycleStatus:"active"}).lean();if(!item?.publishedRevisionId)throw new AppError("Una tappa della visita non e disponibile",409);const itemRevision=await ItemRevision.findById(item.publishedRevisionId).lean();if(!itemRevision)throw new AppError("Revisione pubblicata dell'item non trovata",409);const vocabulary=await getMuseumVocabulary(item.museumId);let representation=null;if(visit.kind==="official"){const policy=effectivePreference.mode==="custom"?effectivePreference:revision.defaultPresentationPolicy;representation=findRepresentationByPolicy(itemRevision,policy)||findDefaultRepresentation(itemRevision)}else if(["custom","user_default"].includes(effectivePreference.mode))representation=resolveCommunityRepresentation({source:itemRevision,durationTypes:vocabulary.durationTypes,languageLevels:vocabulary.languageLevels,preference:effectivePreference});else representation=findDefaultRepresentation(itemRevision);if(!representation)throw new AppError("Impossibile risolvere una representation per una tappa",409);const duration=vocabulary.durationTypes.find(e=>e.key===representation.durationKey);estimatedContentSeconds+=duration?.targetSeconds||0;stops.push({position:index,itemId:item._id,itemRevisionId:itemRevision._id,museumId:item.museumId,variantKey:representation.variantKey||null,representation,targetSeconds:duration?.targetSeconds||null,role:stop.role||"recommended"})}return{visitId:visit._id,visitRevisionId:revision._id,kind:visit.kind,effectivePreference,estimatedContentSeconds,stops}}
module.exports={normalizeAbstractPreference,setUserDefaultPreference,setVisitPreference,getStoredVisitPreference,getVisitPreferenceOptions,getEffectivePreference,buildPresentationPlan};
