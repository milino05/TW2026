const test=require("node:test");
const assert=require("node:assert/strict");
const{normalizeVocabularyPayload,validateVocabularyPayload}=require("../services/validation/vocabulary.validation");
function validVocabulary(){return{languageLevels:[{key:"simple",label:"Semplice"},{key:"advanced",label:"Avanzato"}],durationTypes:[{key:"short",label:"Breve",targetSeconds:30},{key:"long",label:"Lunga",targetSeconds:120}],itemTypes:[{key:"artwork",label:"Opera",capabilities:["navigation_target","spatial_placement","semantic_context"],semanticRefs:[{scheme:"schema.org",id:"VisualArtwork",matchType:"close"}]},{key:"artist",label:"Artista",capabilities:["semantic_context"],semanticRefs:[]}],relationTypes:[{key:"created_by",label:"Creato da",category:"semantic",strength:"strong",directionality:"directed",domain:["artwork"],range:["artist"],semanticRefs:[]}],presentationAspects:[{key:"anecdotal",label:"Aneddotico",semanticRefs:[{scheme:"local",id:"anecdotal",matchType:"exact"}]}],selectionSignals:[{key:"highlight",label:"Highlight",semanticRefs:[{scheme:"local",id:"highlight",matchType:"exact"}]}]}}
test("il vocabolario semantico accetta tipi, relazioni, aspect, selection signal e semanticRefs",()=>{const normalized=normalizeVocabularyPayload(validVocabulary());assert.deepEqual(validateVocabularyPayload(normalized),[])});
test("navigation_target richiede spatial_placement",()=>{const payload=validVocabulary();payload.itemTypes[0].capabilities=["navigation_target"];const errors=validateVocabularyPayload(normalizeVocabularyPayload(payload));assert.equal(errors.some(error=>error.code==="NAVIGATION_TARGET_REQUIRES_PLACEMENT"),true)});
test("domain e range devono riferire ItemType del vocabolario",()=>{const payload=validVocabulary();payload.relationTypes[0].range=["unknown"];const errors=validateVocabularyPayload(normalizeVocabularyPayload(payload));assert.equal(errors.some(error=>error.code==="UNKNOWN_ITEM_TYPE"),true)});
test("gli ItemType devono usare direttamente il nuovo schema strutturato",()=>{const payload=validVocabulary();payload.itemTypes=["artwork"];const errors=validateVocabularyPayload(normalizeVocabularyPayload(payload));assert.equal(errors.some(error=>error.code==="INVALID_TYPE"),true)});
